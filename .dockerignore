# Build artifacts
masterdnsvpn_client
masterdnsvpn_server

# Runtime config / secrets (must be mounted at runtime, never baked into image)
client_config.toml
server_config.toml
*.key

# Logs
*.log

# OS / editor artifacts
.DS_Store
*.swp
*.swo
Thumbs.db

# Git
.git
.gitignore

# Python reference implementation (not needed for Go build)
internal/python_edition/
